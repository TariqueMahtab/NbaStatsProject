using Microsoft.AspNetCore.Identity;

namespace NbaStatsProject.Server.Models
{
    public class NbaStatsUser : IdentityUser
    {
        // You can add more properties here later (like DisplayName, FavoriteTeam, etc.)
    }
}
